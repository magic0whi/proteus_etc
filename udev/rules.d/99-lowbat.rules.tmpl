{{ if $batModel := dig .chezmoi.hostname "batModel" nil . -}}
# Suspend the system when battery level drops to 5% or lower
SUBSYSTEM=="power_supply", ATTR{model_name}=="{{ $batModel }}", ATTR{status}=="Discharging", ATTR{capacity}=="[0-5]", RUN+="/usr/bin/systemctl hibernate"
{{- end }}
